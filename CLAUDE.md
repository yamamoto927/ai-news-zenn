# 生成AIデイリーニュース（Zenn下書き）

このリポジトリは Zenn と GitHub 連携している。`articles/` に Markdown を置いて `main` に push すると Zenn に取り込まれる。
記事はすべて下書き（`published: false`）で作成し、人間が Zenn のダッシュボードで読む。
毎日のルーティン実行では、以下の「日次手順」に厳密に従うこと。

## 1日の投稿構成

1日あたり **最大4本**：

| 種類 | ファイル名 | 本数 |
|---|---|---|
| 重要トピック記事 | `articles/ainews-YYYYMMDD-t1.md` 〜 `-t3.md` | 0〜3本（**最大3本**） |
| その他まとめ | `articles/ainews-YYYYMMDD-misc.md` | 0〜1本 |
| 失敗報告（異常時のみ） | `articles/ainews-YYYYMMDD-error.md` | 異常時のみ1本 |

`YYYYMMDD` は JST の日付（例: `20260925`）。`t1` が最重要。

## 日次手順

1. **日付を確定する**：`TZ=Asia/Tokyo date +%Y%m%d` で JST の今日の日付を求める。
   対象期間は「JST前日 06:00 〜 当日 06:00」の約24時間。
2. **重複チェック**：`articles/ainews-YYYYMMDD-*.md` が1つでも存在すれば、何もせず終了する。
   また直近7日分の記事を読み、既に取り上げたニュースは再掲しない（大きな続報がある場合のみ「続報」として扱う）。
3. **収集**：WebSearch / WebFetch で対象期間のニュースを集める。下記「情報源」を優先する。
   - 各ニュースは **必ず一次情報（公式発表・論文・公式ブログ）か信頼できる報道の記事ページを WebFetch で実際に開いて** 内容と日付を確認する。
   - 開いて確認できなかったニュースは載せない。日付が対象期間外のものも載せない。
   - アクセスできないサイトがあれば、同じニュースを報じている別の媒体で確認を試みる。
4. **選別**：確認できたニュースを重要度順に並べる。優先度の目安：
   主要モデル/製品のリリース > 大型の資金調達・買収・提携 > 規制・政策 > 注目研究 > 開発ツール/OSS > その他。
   - 上位 **最大3件** を「重要トピック記事」にする。単独記事にするほど重要でなければ3件未満でよい。
   - 残り（最大8件程度）を「その他まとめ」に入れる。日本国内の動き（国内企業・政府・日本語モデル）は、該当があれば最低1件入れる。
   - ニュースが少ない日も、確認できたものがあれば必ず記事にする（少なければ「その他まとめ」1本だけでもよい）。
5. **執筆**：下記テンプレートで記事を作成する。
6. **自己検証**：front matter の形式、`published: false`、タイトルが70文字以内、全ニュースに出典URLがあること、日付の整合、本数上限（最大4本）を確認する。
7. **校閲**：Agent ツールで `fact-checker` サブエージェントを呼び出し、今日作成した記事ファイルのパス一覧と対象期間を渡して校閲させる。
   - `[要修正]` の指摘はすべて直す。出典で裏付けられない内容は、表現を直すのではなく削除する。削除でニュースとしての要点が成り立たなくなる場合は、そのニュースごと外す。
   - `[確認推奨]` の指摘は、表現を弱める（「〜と報じられている」等）か削除する。
   - `[要修正]` を直した場合は、もう一度だけ `fact-checker` で校閲する（校閲は最大2回）。2回目でも `[要修正]` が残る箇所は削除する。
   - `fact-checker` を呼び出せなかった場合は、自分で出典と再照合したうえで push し、最終報告に「校閲エージェント未実行」と明記する。
8. **公開**：`git add articles/ && git commit -m "ainews: YYYYMMDD" && git push origin main`

## 特集記事（手動依頼のみ）

日次ニュースとは別に、人間がテーマを指定して1本だけ依頼することがある。依頼プロンプトに「特集記事」とテーマが書かれている場合のみ、日次手順の代わりにこの手順に従う（日次の重複チェック・本数上限は適用しない）。

1. ファイル名は `articles/feature-YYYYMMDD-<英小文字・数字・ハイフンのスラッグ>.md`（全体で12〜50文字）。同名ファイルがあれば末尾に `-2` などを付ける。
2. 依頼で指定されたテーマ・情報源に沿って調べる。情報源には Web ページのほか、このリポジトリ内のファイル（CLAUDE.md、.claude/agents/ など）を使ってよい。
3. 執筆ルールはすべて適用する（`published: false`、出典明記、推測を書かない等）。リポジトリ内のファイルを根拠にした記述は、出典欄にファイルパスを書く。
4. 構成は「特集記事テンプレート」に従う。見出しはテーマに合わせて調整してよい。
5. 日次手順の「自己検証」「校閲」を同様に行い、`git add articles/ && git commit -m "feature: <スラッグ>" && git push origin main` で push する。
6. 失敗した場合は `articles/feature-YYYYMMDD-error.md` に失敗報告を残す。

## 失敗時の手順

人間は Zenn の下書きしか見ないため、**失敗したら必ず失敗報告の下書きを残す**。

- 次のいずれかに該当する場合は、通常記事の代わりに（または通常記事に加えて）`articles/ainews-YYYYMMDD-error.md` を作成して push する：
  - 確認できたニュースが0件だった
  - 情報源の多くにアクセスできなかった
  - その他、手順を最後まで完了できなかった
- 失敗報告には、何を試みたか・どのサイト/コマンドでどんなエラーが出たか・人間が取るべき対応を簡潔に書く。
- main への push 自体が失敗した場合は、別ブランチに push を試み、最終報告にエラー内容を明記する。

## 執筆ルール

- **front matter は必ず `published: false`（下書き）にする。** 公開への切り替えは人間が内容を確認してから手動で行う。
  Zenn の規約は「機械により自動生成された文章の投稿」をスパムとして禁止しているため、ルーティンが `true` にすることは絶対にしない。既存記事の `published` も変更しない。
- タイトルは、Zenn の記事一覧でタイトルだけ見て内容がわかるよう、具体的に書く（誰が・何を・どうした）。
- **タイトルは最大70文字（全角・半角・記号・空白をすべて1文字と数える）。** Zenn は70文字を超えるタイトルをデプロイエラーにするため、超えると記事が反映されない。
  書いたら必ず文字数を数えて確認する（例: `grep -m1 '^title:' <file> | sed 's/^title: *"//;s/"$//' | python3 -c 'import sys; print(len(sys.stdin.read().strip()))'`）。超えていたら副題や列挙を削って短くする。
- 要約は **自分の言葉で** 書く。元記事の文章を丸写ししない。引用は必要な場合のみ1文以内・引用符付き。
- 推測や未確認情報は書かない。噂・リークは「〜と報じられている」と明示し、出典を付ける。
- 数値（価格・パラメータ数・ベンチマーク等）は出典に書かれているものだけを使う。
- 文体はです・ます調。専門用語は初出で簡単に補足する。
- **概要は読みやすさ優先**：要点（誰が・何を・キーとなる数値）を **太字** にし、テンプレートの「発表元／発表日／ポイント」の箇条書きを付ける。太字は多用しない（1文に1〜2か所まで）。
- **「このニュースの影響」**：エンジニア・開発者、一般の利用者、企業・ビジネスなど、誰にどんな影響があるかを対象ごとに書く。関係の薄い対象は書かない。
  出典に書かれた事実（料金・提供範囲・仕様変更など）から素直に導ける影響だけを書き、出典にない新しい事実（時期・数値・未発表の機能など）を付け足さない。見込みを書く場合は「〜が考えられます」「〜する可能性があります」と明示する。
- **「キーワード」**：記事に関連する技術用語・製品名・企業名・分野をタグのようにインラインコード（`` `RAG` `` のような形式）で並べる。重要トピック記事は5〜8個、その他まとめは各ニュース3〜5個。記事本文に出てくる語、またはその分野の一般的な用語を使う。

## テンプレート：重要トピック記事

ファイル名: `articles/ainews-YYYYMMDD-t1.md`（t2, t3 も同形式）

```markdown
---
title: "<具体的な見出し。例：Anthropic、Claude Opus 5.5を発表 — Opus 5比で40%低コスト>"
emoji: "📰"
type: "idea"
topics: ["ai", "生成ai", "llm", "news"]
published: false
---

:::message
YYYY/MM/DD の生成AIニュース（重要トピック N/3）
:::

## 概要

<何が起きたかを3〜4文で。誰が・何を・いつ・キーとなる数値など要点を **太字** にする（1文に1〜2か所まで）>

- **発表元**：<企業・組織名>
- **発表日**：YYYY/MM/DD
- **ポイント**：<一言で>

## このニュースの影響

<このニュースで誰に・どんな影響があるかを、対象ごとに箇条書きで。該当する対象だけ書く（2〜4項目）>

- **エンジニア・開発者**：<例：API の料金・仕様変更で何が変わるか、移行作業の要否>
- **一般の利用者**：<例：使えるアプリ・機能・料金がどう変わるか>
- **企業・ビジネス**：<例：導入判断・コスト・競争環境への影響>

## 詳しく

<発表内容のポイントを箇条書きや短い段落で。出典に書かれている事実のみ>

## なぜ重要か

<業界全体の流れの中での位置づけを2〜4文で>

## キーワード

`<技術ワード1>` `<技術ワード2>` `<製品名・企業名>` ...（5〜8個）

## 出典

- [<媒体名：記事タイトル>](<URL>)
- [<媒体名：記事タイトル>](<URL>)

---

*この記事は Claude により公開情報をもとに自動生成されています。詳細・正確な情報は各出典をご確認ください。*
```

## テンプレート：その他まとめ

ファイル名: `articles/ainews-YYYYMMDD-misc.md`

```markdown
---
title: "生成AIニュース YYYY/MM/DD その他まとめ：<主な話題を2〜3個>"
emoji: "🗞️"
type: "idea"
topics: ["ai", "生成ai", "llm", "news"]
published: false
---

## ニュース一覧

### <見出し>

<要約 2〜3文。要点を **太字** にする>

**このニュースの影響**：<誰に・どんな影響があるかを1〜2文で（例：エンジニアは〜、利用者は〜）>

**なぜ重要か**：<1文>

**キーワード**：`<技術ワード>` `<技術ワード>` `<製品名>`（3〜5個）

出典：[<媒体名>](<URL>)

### <見出し>

...

---

*この記事は Claude により公開情報をもとに自動生成されています。詳細・正確な情報は各出典をご確認ください。*
```

## テンプレート：特集記事

ファイル名: `articles/feature-YYYYMMDD-<スラッグ>.md`

```markdown
---
title: "<テーマが具体的にわかるタイトル>"
emoji: "📝"
type: "idea"
topics: ["ai", "生成ai", "<テーマに合うトピック>"]
published: false
---

:::message
特集記事（YYYY/MM/DD）
:::

## はじめに

<この記事で何を紹介するか 2〜3文>

## <本文の見出し（テーマに合わせて複数）>

<本文>

## まとめ

<要点を2〜4文で>

## 出典・参考

- [<名前>](<URL>)
- リポジトリ内ファイル：`<パス>`

---

*この記事は Claude により自動生成されています。詳細・正確な情報は各出典をご確認ください。*
```

## テンプレート：失敗報告

ファイル名: `articles/ainews-YYYYMMDD-error.md`

```markdown
---
title: "【要確認】YYYY/MM/DD 生成AIニュースの作成に失敗"
emoji: "⚠️"
type: "idea"
topics: ["ai", "news"]
published: false
---

## 何が起きたか

<1〜3文>

## 詳細

- <アクセスできなかったサイトやエラー内容>

## 必要な対応

<人間が取るべき対応>
```

## 情報源（優先）

- 公式：Anthropic News、OpenAI News、Google DeepMind / Google AI Blog、Meta AI Blog、Microsoft AI Blog、Hugging Face Blog、Mistral AI News、xAI
- 海外報道：TechCrunch (AI)、The Verge (AI)、VentureBeat (AI)、Reuters (Technology)、Ars Technica
- 国内報道：ITmedia AI+、Impress Watch、日経クロステック
- 研究：arXiv（cs.CL / cs.LG の注目論文）、Hugging Face Papers
